# Local development — copy to local.settings.json and fill in values
# Do NOT commit local.settings.json

# Azure Functions custom handler port (must match host.json)
CUSTOM_HANDLER_PORT=8000

# Optional: API keys for tools that call external services
# MY_API_KEY=your_key_here
