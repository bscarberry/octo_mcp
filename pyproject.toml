[project]
name = "octo-mcp-server"
version = "1.0.0"
description = "MCP tools hosted with the Azure Functions MCP extension"
requires-python = ">=3.11"
dependencies = [
    "azure-functions>=1.24.0",
    "httpx",
]
